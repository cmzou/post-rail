[project]
name = "post-rail"
version = "0.1.0"
description = "Application to manipulate image files."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "ruff>=0.15.7",
]
